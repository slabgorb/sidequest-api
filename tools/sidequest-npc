#!/usr/bin/env bash
# Wrapper for sidequest-namegen — generates NPC identity from genre pack data.
# Reads SIDEQUEST_CONTENT_PATH and SIDEQUEST_GENRE from env (set by orchestrator).
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
BINARY="${SCRIPT_DIR}/../target/debug/sidequest-namegen"

if [[ ! -x "$BINARY" ]]; then
  BINARY="${SCRIPT_DIR}/../target/release/sidequest-namegen"
fi

if [[ ! -x "$BINARY" ]]; then
  echo "ERROR: sidequest-namegen binary not found" >&2
  exit 1
fi

exec "$BINARY" --genre-packs-path "${SIDEQUEST_CONTENT_PATH}" --genre "${SIDEQUEST_GENRE}" "$@"
